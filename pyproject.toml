[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "unified-mandala-demo"
version = "1.0.0"
description = "Demo/visualization layer for unified-mandala: a holistic, modular framework for symbolic AI, CREP logic, and ethical agent workflows."
authors = [{ name = "Johann Römer" }]
license = { text = "GPL-3.0-or-later" }
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[tool.setuptools]
packages = []
